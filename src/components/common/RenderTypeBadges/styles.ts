import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  badge: {
    width: "75%",
    borderRadius: 8,
  },
  badgeText: {
    color: "white",
    fontWeight: "bold",
    textAlign: "center",
  },
});
