import { StyleSheet, } from "react-native";


export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 74,
    position: "absolute",
    bottom: 0,
    left: 0,
    borderTopWidth: 1,
    borderTopColor: 'red',
    justifyContent: "center",
    alignItems: "center",
  },
  textContainer: {
    color:'black',
  },
  buttonContainer: {
    fontWeight: "bold",
  },
});